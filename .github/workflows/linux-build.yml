name: Linux build

# Builds the Linux packages (.deb + AppImage + .rpm) for the Tauri app in
# windows/ (the cross-platform desktop app; the folder name is historical).
# Trigger manually from the Actions tab (workflow_dispatch) to test, or push a
# tag like `linux-v0.1.2` to also attach the packages to a GitHub release.
on:
  workflow_dispatch:
  push:
    tags:
      - "linux-v*"

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    defaults:
      run:
        working-directory: windows
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: windows/package-lock.json

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: windows/src-tauri

      # Tauri v2 Linux build/runtime deps: webkit2gtk 4.1, GTK, the Ayatana
      # appindicator for the tray, librsvg for icons, and patchelf for AppImage.
      - name: Install Linux build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            file \
            libssl-dev \
            build-essential \
            curl wget

      - name: Install frontend deps
        run: npm ci

      # On a linux-v* tag, also emit the AppImage updater artifacts (.tar.gz +
      # .sig) for auto-update. Needs the TAURI_SIGNING_PRIVATE_KEY secret; plain
      # (workflow_dispatch) builds skip signing and just produce the packages.
      - name: Build Tauri app (deb + appimage + rpm)
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ""
        run: |
          if [[ "${{ github.ref }}" == refs/tags/linux-v* ]]; then
            npm run tauri build -- --bundles deb,appimage,rpm --config '{"bundle":{"createUpdaterArtifacts":true}}'
          else
            npm run tauri build -- --bundles deb,appimage,rpm
          fi

      - name: Upload packages as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: agentpet-linux-packages
          path: |
            windows/src-tauri/target/release/bundle/deb/*.deb
            windows/src-tauri/target/release/bundle/appimage/*.AppImage
            windows/src-tauri/target/release/bundle/rpm/*.rpm
          if-no-files-found: error

      - name: Attach packages + updater artifacts to release (on linux-v* tag)
        if: startsWith(github.ref, 'refs/tags/linux-v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            windows/src-tauri/target/release/bundle/deb/*.deb
            windows/src-tauri/target/release/bundle/appimage/*.AppImage
            windows/src-tauri/target/release/bundle/appimage/*.AppImage.tar.gz
            windows/src-tauri/target/release/bundle/appimage/*.AppImage.tar.gz.sig
            windows/src-tauri/target/release/bundle/rpm/*.rpm
